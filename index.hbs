<!DOCTYPE html>
<html>
  <head>
    <title>Cook | Clank</title>
  </head>
    <style>
      body * { 
        font-size: 26px; 
        text-decoration: none;
        font-family: monospace;
      }

      body > a {
        text-transform: capitalize;
        border: solid 1px blue;
        padding: 1em;
        margin: 0.5em 5px;
        display: inline-block;
      }

      header > a {
        float: right;
        padding-right: 20px
      }
    </style>
    <header>
      <a href="https://github.com/mrmicahcooper/recipes">README.md</a>
      <h1>Cook | Clank</h1>
    </header>

    <body>
      {{#each recipes}}
        <a href="{{path}}">{{name}}</a>
      {{/each}}
    </body>


</html>
